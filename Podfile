# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhDProjectPractive' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhDProjectPractive
pod'Kommunicate'

  target 'PhDProjectPractiveTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhDProjectPractiveUITests' do
    # Pods for testing
  end

end
